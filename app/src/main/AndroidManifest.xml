<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Stories.StoryList">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.StoryList1">
<!--      android:theme="@style/Theme.StoryList1">-->

    <activity android:name="com.stories.StoryList.UserProfile"></activity>
    <activity android:name="com.stories.StoryList.NewUserRegistration" />
    <activity android:name="com.stories.StoryList.UserLogin" />
    <activity android:name="com.stories.StoryList.AddNewStory" />
    <activity android:name="com.stories.StoryList.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <meta-data
        android:name="preloaded_fonts"
        android:resource="@array/preloaded_fonts" />
  </application>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.INTERNET" />

  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>